import React from "react";

function Note() {
    return ( <
        div className = "note" >
        <
        h1 > New note < /h1> <
        p > Hello, World! < /p> < /
        div >
    );
};

export default Note;